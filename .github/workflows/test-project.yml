 name: Install NodeJS
 on: [push]
 jobs:
   test:
     runs-on: ubuntu-latest
     steps:
       - name: Get Code
         uses: actions/checkout@v3
       - name: Install Flask
         uses: actions/setup-flask
         with:
           node-version: 18
